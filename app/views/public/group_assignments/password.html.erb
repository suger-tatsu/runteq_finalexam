<h1 class="text-3xl font-bold text-gray-800 mb-6 text-center">パスワード認証</h1>

<div class="max-w-2xl mx-auto bg-white p-6 rounded shadow">
  <p class="mb-6 text-gray-700 text-center">
    このグループ分け結果を見るにはパスワードを入力してください。
  </p>

  <%= form_with url: public_verify_password_group_assignment_path(token: @group_assignment.public_token), local: true do |form| %>
    <div class="mb-6">
      <%= form.label :password, "パスワード", class: "block mb-2 font-semibold text-gray-700" %>
      <%= form.password_field :password, class: "w-full border border-gray-300 rounded p-3" %>
    </div>

    <div class="flex justify-center">
      <%= form.submit "認証する", class: "bg-blue-500 hover:bg-blue-600 text-white px-6 py-3 rounded shadow" %>
    </div>
  <% end %>

  <% if flash[:alert] %>
    <div class="mt-6 text-red-600 text-center font-semibold">
      <%= flash[:alert] %>
    </div>
  <% end %>
</div>